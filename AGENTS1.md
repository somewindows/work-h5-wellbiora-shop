# H5 跨境电商商城 — 项目说明（AGENTS.md）

> 本文件供 AI 编码助手（Kimi Code）在每次会话中读取，作为项目长期上下文。
> 修改业务规则时请同步更新本文件。

## 一、项目背景

- 跨境电商 H5 商城：**移动端网页（非微信小程序）**，销售海外保健品。
- 清关模式：**1210 保税备货**。
- 后端对接：**君梦 OMS OpenAPI 2.0**（义乌保税仓），JSON + MD5 签名（参数按 key 排序拼接 + appSecret 后转大写）。未来可能扩展**成都保税仓**。
- 支付：**微信支付**（含海关报关接口）。
- 设计稿未定，产品图片与参考小程序截图存放于 `docs/` 或 `prototype/` 目录，作为设计依据。

## 二、技术栈约定（生成代码必须遵循，不要换框架）

### 前端
- Vue 3 + Vite + TypeScript + Vant 4 + TailwindCSS + Pinia + Vue Router（**hash 模式**）+ Axios + Swiper
- px 用 `postcss-px-to-viewport` 转 vw，**375px 为设计基准**

### 后端
- Node.js + NestJS + TypeScript + MySQL 8 + Redis

### 部署
- 国内服务器 + Nginx + HTTPS + Docker

## 三、硬性规则（不可违反）

1. **密钥安全**：appSecret、支付密钥等敏感信息只允许在服务端，前端代码绝不出现。
2. **下单硬校验**（跨境电商零售进口限额）：
   - 实名认证（姓名 + 身份证号）
   - 单笔订单 ≤ 5000 元
   - 个人年度累计 ≤ 26000 元
3. **文案合规红线**：商品文案不得出现疾病治疗、预防、医疗术语（保健品广告合规）。
4. **移动端优先**：适配 iPhone 安全区 `env(safe-area-inset-bottom)`，固定底部栏不被遮挡。
5. **保税仓统一适配层**：所有保税仓对接走统一适配层封装，前端只调我们自己的接口，方便未来扩展成都仓。
6. **三单对碰**：订购人、支付人、收货人实名信息必须一致的校验逻辑要保留（海关要求）。
7. **页面架构**：商品详情页和首页一律采用「内容块/模块数组 + 前端渲染器」架构，**禁止用富文本字段堆砌详情页**；新增内容块类型时先给出 type 定义和渲染组件，再写页面。
8. **设计规范**：所有页面样式以 `docs/H5商城设计规范_WELLBIORA_v0.2.md` 为准（色值、圆角、间距、字号不得自行发挥）；产品图未提供前统一用占位图路径 `/assets/placeholder/`。

## 四、协作方式

- 全程用中文回答，代码注释用中文。
- 每个功能按「文件结构 → 完整可运行代码 → 要点说明」输出，标注每个文件的存放路径。
- 改已有代码时，只给改动部分并说明替换了什么，不要每次重发全量文件。
- 涉及君梦接口时，主动提醒：**测试环境先行、签名调试、`orderDeclaNo` 和 `plaformCode`（注意君梦文档原始拼写）等待确认项**。
- 不确定的需求，先向我确认再写代码，不要自行假设业务规则。

## 五、目录约定

```
H5-shop/
├── docs/                  # 设计规范、需求文档、参考截图
├── prototype/             # 早期 HTML 静态原型（v1/v2，仅作视觉参考）
├── assets/                # 产品图片素材（wellbiora 品牌）
├── frontend/              # Vue 3 H5 前端（待建）
└── server/                # NestJS 后端（待建）
```

## 六、待确认事项（碰到来问我）

- [ ] 君梦 OMS 测试环境地址与测试账号
- [ ] 君梦接口字段：`orderDeclaNo`、`plaformCode` 的具体含义与取值
- [ ] 微信支付商户号、报关接口接入方式（服务商模式还是直连）
- [ ] 成都保税仓的 OMS 对接方是否也是君梦
- [ ] 设计稿最终确认版本
