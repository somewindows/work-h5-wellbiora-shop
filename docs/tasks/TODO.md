# 待办任务（TODO）

> 本文件是当前活跃任务清单，AI 助手每次会话开工前读这里。
> 任务完成后：把该任务条目**剪切**到 `archive/` 下按日期归档，并从本文件删除。
> `archive/` 里的内容**不需要读**（节省上下文），除非用户明确要求回溯。

## 活跃任务

### T1 · 个人中心（我的）页原型设计
- **状态**：等待素材
- **目标**：设计 MVP 第 8 页「我的」，只做最基本链路的核心功能
- **待用户提供**：2 个小程序个人中心参考（截图 + 文字/功能说明）
- **设计依据**：`docs/H5商城设计规范_WELLBIORA_v0.2.md`（v0.3）第七节 #8：用户信息、我的订单入口、联系客服、协议入口
- **风格基线**：延续 `prototype/app/` V2 样式令牌；参考已完成的 `designs/h5-shop/购物车 Cart.html` 的做法（单文件自包含 HTML + 本地 assets/ + 演示开关）
- **产出位置**：`designs/h5-shop/`
- **工具**：`.agents/skills/baoyu-design/`（SKILL.md 为入口；本机无 Node，跳过 `agents/*.mjs` 脚本）
- **预览**：`python -m http.server 4311` 后访问 `http://localhost:4311/designs/h5-shop/<文件>.html`
- **开工前需向用户确认**：链路范围——是否包含「订单列表 → 订单详情」两页（若是，则拆成多个任务）

## 完成后的后续任务（排期未定，先占位）

- T2 · 结算页原型（MVP #5）
- T3 · 地址+实名页原型（MVP #6）
- T4 · 订单列表/详情原型（MVP #7）
- T5 · 前端工程初始化（Vue 3 + Vite + TS + Vant 4，需先装 Node.js）
