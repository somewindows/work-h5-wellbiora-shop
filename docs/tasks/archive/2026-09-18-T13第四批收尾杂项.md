# T13 第 4 批 · 收尾杂项（2026-09-18 完成）

> 评审报告（docs/project-review/2026.09.12-project-review.md）第 4 批剩余项的处置记录。

## 已完成

- **R17 备份脚本**：`server/scripts/backup.ps1` 与部署指南 17.3 的 mysqldump 输出从 PowerShell `>` 重定向（PS 5.1 会转 UTF-16/BOM 写坏 SQL）改为 `--result-file=`；顺手修回脚本丢失的 UTF-8 BOM（PS 5.1 无 BOM 会把中文按 GBK 解析）。**恢复演练已过**（便携 MySQL 8.4）：备份 wellbiora_shop → 恢复至 scratch 库 → 表数 16/16、关键表行数一致、迁移表 16/16。
- **依赖告警清零（server）**：qs 6.15.3→6.16.0（audit fix）；multer 2.2.0→2.4.0（@nestjs/platform-express 11 锁死 2.2.0，用 package.json `overrides` 强制升级——5 个 high 全部清除，上传功能测试 265/265 + e2e 60/60 回归通过）。`npm audit` server 端 0 漏洞。
- **Swiper 移除**：frontend 全程零引用，从 dependencies 移除，frontend critical 告警消除（剩余 5 条为 vite/vitest/esbuild 开发链，仅开发期暴露，修复需大版本升级，接受）。
- **H5 401 自动登出**：`frontend/src/api/request.ts` 响应拦截器全局处理——401 清本地 token + 跳 `/#/login?from=<来源>`（登录页自身除外防循环；node 测试环境有 window 守卫）。订单详情错误区分：仅 40404 进「订单不存在」空态，其余 toast。
- **结算页金额口径**：展示改用服务端预检快照 `payableFen`（失败回落购物车合计；服务端下单仍重新计价，安全不变）。
- **登录页 mock 提示**：「任意 6 位可登录」仅在 `USE_MOCK_MODE` 下显示。
- **管理员会话隔离**：后台 JWT 独立 `ADMIN_JWT_SERVICE`（`server/src/admin/admin-jwt.ts`）——`ADMIN_JWT_SECRET` 可配（缺省回落 JWT_SECRET + 告警，不破坏已部署环境），有效期 `ADMIN_JWT_EXPIRES` 默认 12h（原随用户侧 7d）；撤销已由守卫回查库覆盖（禁用/改密即时生效）。
- **全局异常定位日志**：`http-exception.filter.ts` 补 5xx 告警/未预期异常堆栈日志，只记 method+originalUrl，不记 body/headers（防验证码/证件/密钥落日志）。

## 核实不改（有结论）

- **v-html 两处**（CheckoutView 协议正文、BlockCertWall 图标）：渲染源均为站内本地常量、非用户输入，评审原文也注明「不报告为已证实 XSS」。协议正文待法务审定后重写时再结构化，本次不改。
- **R11/R12 剩余**（微信响应验签/请求超时/证书刷新放大）：支付链路加固项，公钥模式下 R11 证书刷新分支不可达（deepseek 复核结论）；属 `server/src/payments/` 改动，按评审关流程留给下一批集中处理。
- **客服/协议/收货操作占位**：需负责人提供客服联系方式与协议定稿文案，非代码项。
- **商品列表内存分页**：4 款商品无实际影响，规模增长时再改数据库分页。

## 验证

- server 单测 265/265、e2e 60/60、lint、build 全绿（multer/qs 升级后全量回归通过）
- frontend vitest 28/28（新增 loginRedirectHash 2 例）、type-check、build 全绿
