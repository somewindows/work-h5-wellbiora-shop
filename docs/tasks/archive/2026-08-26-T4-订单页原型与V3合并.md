# 2026-08-26 · T4 订单列表/详情原型 + 原型统一为 V3（已完成）

## 产出

- **T4 新页面**：`prototype/app/orders.html`（订单列表，状态 Tab：全部/待付款/待发货/待收货/已完成，支持 `?tab=` 参数直达）+ `prototype/app/order.html`（订单详情：状态头/物流时间线/地址/商品/金额明细/订单信息/按状态的底部操作栏）
- **共享数据**：`prototype/app/orders-data.js`（4 笔 mock 订单覆盖全部状态，含物流轨迹、海关申报单号字段）
- **V3 合并**：`designs/h5-shop/` 4 页（cart/checkout/address/mine）迁入 `prototype/app/`，`designs/` 目录删除；全部相对路径重写
- **链路打通**：8 页互链全部可点，无占位断点——index-v2/products-v2/product-v2 的 Tabbar 与购买栏接入 cart/checkout/mine；cart 去结算 → checkout；checkout 支付成功 → orders；mine 订单入口 → orders.html?tab=*
- **验证**：本地 HTTP 服务逐页 + 素材 curl 全部 200；残留「演示占位」仅剩 V1 历史页与 MVP 范围外项（登录页、协议正文、退款售后）

## 设计假设待确认（规范外补充）

- 订单状态机取最小集：pay / ship / recv / done（取消、退款售后留后续迭代）
- mine.html 的「待评价」入口映射到「已完成」Tab
- 订单号/海关申报单号字段格式为 mock（`WB*` / `HG*`），真实格式待君梦接口确认

## 遗留点

- 订单相关操作（取消二次确认、提醒发货）均为 toast 占位
- checkout.html 支付成功后跳转 orders.html，不会真实生成订单（纯静态原型的固有限制）
