# 系统文件
Thumbs.db
.DS_Store
desktop.ini

# 依赖与构建产物（后续前后端工程用）
node_modules/
dist/
*.log

# 本地环境配置
.env
.env.local
.env.*.local

# 支付证书与密钥（微信支付商户证书等，绝不入库）
server/certs/
*.pem
*.p12

# 后台上传的商品图片（本地开发落盘目录，生产在站点目录外独立存放）
server/uploads/

# QA 截图产物与本地字体构建环境（frontend/）
frontend/qa/
frontend/.venv-fonts/
frontend/.font-cache/
