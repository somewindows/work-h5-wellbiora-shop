# 复制为 .env.development.local 后，开发服务器会改为访问本机 NestJS 服务端。
# 注意：Vite 的 env 优先级是 .env.development.local > .env.development > .env.local，
# 所以覆盖 .env.development 必须用 .env.development.local，.env.local 不生效。
VITE_USE_MOCK=0
